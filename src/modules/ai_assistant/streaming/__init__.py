"""SSE event protocol and encoding for streaming an agent run to the client.

See `events.py` for the 9 named events and their payloads, `sse.py` for
turning an `SSEEvent` stream into `text/event-stream` bytes.
"""
