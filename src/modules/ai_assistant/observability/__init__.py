"""Admin-only, cross-user monitoring for the AI assistant: system-wide
usage over time, provider/model breakdowns, and a per-user usage table.
The single-user counterpart lives in `ai_assistant.permissions` — that
module answers "what can this one user do"; this one answers "what is
everyone doing".
"""
